namespace: monitoring

bases:
  - vendor/bases/monitoring/grafana

configMapGenerator:
  - name: sighup-sample-dashboard-definition
    files:
      - sighup-sample-dashboard.json

patches:
  - add-dashboard.yml
